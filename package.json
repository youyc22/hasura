{
  "name": "eesast-hasura",
  "dependencies": {
    "hasura-cli": "^2.2.0"
  },
  "scripts": {
    "apply": "hasura migrate apply && hasura metadata apply",
    "console": "hasura console",
    "hasura": "hasura",
    "migrate-squash": "hasura migrate squash",
    "status": "hasura migrate status"
  },
  "private": true
}
